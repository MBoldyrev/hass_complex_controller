complex_controller:
  name: hallway
  timer: hallway_timer
  base:
    type: dim
    config:
      scene_on: hallway_day_on
      scene_dim: hallway_day_dim
      scene_off: hallway_off
      duration_on: 90
      duration_dim: 30
    overrides:
      - condition:
          condition: and
          conditions:
            - condition: time
              before: '11:00:00'
              after: '23:00:00'
            - condition: sun
              after: sunset
        type: simple
        config:
          scene_on: hallway_night_on
          scene_off: hallway_off
          duration_on: 90
